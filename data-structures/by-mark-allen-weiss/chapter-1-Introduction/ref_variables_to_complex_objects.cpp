/*
Intro -
int a = 10;
int &b = a;
here 'b' is a refernce variable.

Concept -
Reference variables can also to used to point to heavy objects
within a list if they are frequently needed in code.
This prevents the calculation overhead for each usage, and
object is directly accessible by reference.
*/
